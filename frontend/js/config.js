// Change this to your deployed backend URL (e.g. Render) when going live
//const API_BASE_URL = "http://localhost:5001/api";

const API_BASE_URL = "https://fwc-crackers.onrender.com/api";

// Razorpay key is fetched dynamically from backend at checkout time,
// but you can hardcode it here too if you prefer:
// const RAZORPAY_KEY_ID = "rzp_test_xxxxxxxxxxxx";
